#list all users who are currently logged in
who 

#list all users who are logged in
who -a

#to get currest logged-in user name
whoami

#switch to root user
su

#switch to a specific user
su username

#change password of root user
sudo passwd root

#change password of a specific user
passwd username
passwd

#add new user
sudo adduser newusername
eg. sudo adduser Pallavi
User will be created whose primary group created is pallavi.

#add new group
sudo addgroup newgroupname
eg. sudo addgroup bigdata

#add new user to an existing group
sudo adduser newusername –ingroup exisitinggroupname
eg. sudo adduser xyz –ingroup bigdata

#add an existing user to an existing group
sudo adduser existinguser existingroup
eg. sudo adduser pallavi bigdata

#add user to sudo group
sudo adduser anyusername sudo
eg. sudo adduser pallavi sudo

#change primary group of user
sudo adduser username –g new_primary_groupname
sudo adduser pallavi –g bigdata

#delete user
sudo deluser username

#remove user from a group
sudo deluser username groupname



